apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-service
  namespace: $NAMESPACE
  labels:
    app: $NAMESPACE
    service: schedule
spec:
  selector:
    matchLabels:
      service: schedule
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        service: schedule
    spec:
      containers:
        - name: schedule-service
          image: $REGISTRY/$NAMESPACE/schedule-service:SNAPSHOT-$BUILD_NUMBER
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 1000m
              memory: 4096Mi
            limits:
              cpu: 1500m
              memory: 6144Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 30106
            initialDelaySeconds: 120
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 30106
            initialDelaySeconds: 120
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
            periodSeconds: 10
          ports:
            - containerPort: 30106
              name: http
          envFrom:
            - configMapRef:
                name: zhoil-yn-baseinfo
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: schedule-service
  namespace: $NAMESPACE
spec:
  selector:
    service: schedule
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 30106
      targetPort: 30106


