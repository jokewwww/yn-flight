apiVersion: apps/v1
kind: Deployment
metadata:
  name: statistical-service
  namespace: $NAMESPACE
  labels:
    app:  $NAMESPACE
    service: statistical
spec:
  selector:
    matchLabels:
      service: statistical
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        service: statistical
    spec:
      containers:
      - name:  statistical-service
        image:  $REGISTRY/$NAMESPACE/statistical-service:SNAPSHOT-$BUILD_NUMBER
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 1000m
            memory: 4096Mi
          limits:
            cpu: 1500m
            memory: 6144Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 30103
          initialDelaySeconds: 120
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 30103
          initialDelaySeconds: 120
          timeoutSeconds: 2
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        ports:
        - containerPort:  30103
          name:  http
        envFrom:
        - configMapRef:
            name: zhoil-yn-baseinfo
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: statistical-service
  namespace: $NAMESPACE
spec:
  selector:
    service: statistical
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 30103
    targetPort: 30103