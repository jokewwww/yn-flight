apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobschedual-service
  namespace: $NAMESPACE
  labels:
    app:  $NAMESPACE
    service: jobschedual
spec:
  selector:
    matchLabels:
      service: jobschedual
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        service: jobschedual
    spec:
      containers:
      - name:  jobschedual-service
        image:  $REGISTRY/$NAMESPACE/jobschedual-service:SNAPSHOT-$BUILD_NUMBER
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 1000m
            memory: 4096Mi
          limits:
            cpu: 1500m
            memory: 6144Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 30104
          initialDelaySeconds: 120
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 30104
          initialDelaySeconds: 120
          timeoutSeconds: 2
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        ports:
        - containerPort:  30104
          name:  http
        envFrom:
        - configMapRef:
            name: zhoil-yn-baseinfo
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: jobschedual-service
  namespace: $NAMESPACE
spec:
  selector:
    service: jobschedual
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 30104
    targetPort: 30104
    nodePort: 30104